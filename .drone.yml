kind: pipeline
name: default
type: kubernetes

steps:
  - name: build
    image: python:3.11.2
    commands:
      - pip install -r requirements.txt
      - pytest --no-header -v --junit-xml results.xml
    resources:
      limits:
        cpu: 1000
        memory: 2GiB
